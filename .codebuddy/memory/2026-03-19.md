# 2026-03-19

## enhance-qa-system 设计文档完成

完成了 POE Data Miner 问答系统增强的完整设计文档，位于 `openspec/changes/enhance-qa-system/`。

### 核心决策
1. **解读层预计算入库**：查不到说明设计有漏洞，不做动态兜底
2. **lupa 运行 StatDescriber.lua**：~50行适配层注入 LoadModule/copyTable/round/floor，100%精确
3. **问题分类由 AI 完成**：skill.md 定义 8 种类型识别规则，AI 自动分类
4. **辅助推荐用抽象公式**：可量化的用公式表达差异，不可量化的给潜力推荐
5. **系统定位升级**：从"静态数据查询"→"数据分析汇总服务"

### 4 个 artifact
- `proposal.md`: 5 个 new capability（entity-enrichment/mechanism-enrichment/support-matching/query-output-layer/stat-describer-bridge）
- `design.md`: 6 个关键技术决策 + 数据流架构 + 初始化 7 步流程
- `specs/`: 5 个 spec 文件，共 ~30 个 requirement
- `tasks.md`: 8 个任务组，共 37 个实现任务

### 关键技术发现
- POB 五层数据管线：数据定义→数值构建→映射→Mod合并→计算输出
- StatDescriber.lua 外部依赖仅 5 个函数，适配层工作量小
- 技能专属 statMap 覆盖是提取 summary 独特性的关键来源
- POB 不使用 "mechanism" 术语，机制行为需从代码 Flag/Sum/Mod 逆向提炼

## enhance-qa-system Phase 1 + Phase 2 + Phase 2a 实现完成

### Phase 1 (Tasks 1.1-1.4) 已完成
- entity_index.py `_row_to_dict` 扩展到 22 个 JSON 字段
- formula_matcher.py 移除 200 字符截断
- kb_query.py 添加 5 种实体类型字段裁剪 + 4 种 detail 级别 + response_type 推断

### Phase 2 (Tasks 2.1-2.6) 已完成
- `stat_describer_bridge.py` 639行：lupa LuaRuntime + LoadModule/copyTable/round/floor/ConPrintf 适配层
- 批量预加载 5 个公共 scope，初始化 0.1s
- 优雅降级：lupa 不可用时返回 None + 警告日志

### Phase 2a (Tasks 3.1-3.6) 已完成
- entities 表 DDL v4：新增 summary/key_mechanics/display_stats 三个 TEXT 列
- EntityEnricher 类（entity_index.py 内）：enrich_all + 五种实体类型的提取策略
- summary 来源：技能专属 statMap key → constant_stats → description 首句
- key_mechanics：结构化 JSON [{name, stat, formula, effect}]，仅 skill_definition
- display_stats：StatDescriber 桥接 > stat_descriptions 字段 > 唯一物品 stats 文本
- init_knowledge_base.py 新增 Step 2（enrich_entities）
- schemas.json 更新到 v4.0

### 覆盖率实测数据
- 总计 16,461 实体
- summary: 8,522 (51%) — skill 96%, passive 100%, unique 99%, mod 98%, gem 0%
- key_mechanics: 517 (3%) — 仅 skill_definition 中 41% 有独特 statMap
- display_stats: 8,302 (50%) — 含 StatDescriber 桥接后 skill 78%, passive 100%, unique 99%, mod 98%
- 全量 enrichment 耗时 0.6s

## enhance-qa-system Phase 2b 实现完成 (Tasks 4.1-4.8)

### 机制库增强

- `mechanism_extractor.py` 完全重写为 v2：
  - mechanisms 表新增 5 个字段：friendly_name/behavior_description/mechanism_category/formula_abstract/affected_stats
  - KNOWN_MECHANISMS 字典：46 个机制完整定义（含中文名、分类、公式、affected_stats）
  - BehaviorExtractor 类：三型行为提取策略（Flag/Numeric/Trigger）
  - MECHANISM_RELATIONS 列表：19 条机制间关系定义
  - mechanism_relations 表：6 种关系类型（mutually_exclusive/modifies/requires/overrides/converts/stacks_with）
- `config/mechanism_descriptions.yaml`：33 条 YAML 补充描述
- `init_knowledge_base.py` 更新：extract_mechanisms 传入 pob_path，调用行为提取+YAML加载
- `schemas.json` 更新：mechanisms hash v2.0，新增 mechanism_relations schema

### 覆盖率实测数据
- 总计 71 个机制（46 已知 + 25 模式匹配发现）
- friendly_name: 46/71 (64%)
- mechanism_category: 71/71 (100%)
- behavior_description: 36/71 (50%) — 21 代码提取 + 33 YAML补充（部分重叠）
- formula_abstract: 39/71 (54%)
- affected_stats: 46/71 (64%)
- mechanism_relations: 19 条
- 分类分布：conversion 42, damage_modifier 7, immunity 6, leech 5, resource 4, trigger 4, block 3

### 质量检查修复
- 修复 `_print_stats` 统计口径不一致：friendly_name 改为 `!= m`（与 DB 口径一致），behavior_description 改为读取合并后结果
- 数据库完整性验证通过：0 孤立外键（relations + sources）
- 数据库实际覆盖率：friendly_name 71/71(100%，其中46个中文名), behavior_desc 36/71(51%), category 71/71(100%), formula_abstract 39/71(55%), affected_stats 46/71(65%)

## enhance-qa-system Phase 2d 实现完成 (Tasks 5.1-5.7)

### 辅助匹配系统
- `support_matcher.py` 620行：SupportMatcher 类，RPN表达式求值，兼容矩阵+效果分类+潜力推荐
- supports.db 3个表：support_compatibility(52,605对)、support_effects(556个,15类)、support_potential(9,762条)
- 可量化辅助：279/556 (50%)，效果分类分布：utility 173, damage_more 95, dot 48, duration 44, speed 28...
- 潜力推荐：8条MECHANIC_SYNERGY_RULES，3种synergy_type(mechanic_match/tag_synergy/stat_amplify)

## enhance-qa-system Phase 3 实现完成 (Tasks 6.1-6.7)

### 查询输出层
- `kb_query.py` 完全重写为 v3 (~1110行)
- 新增子命令：supports(4模式)/compare(3级)/reverse-stat/formula --chain
- entity --detail (summary/levels/stats/full)，mechanism --detail (behavior/relations/full)
- 所有查询附加 response_type 字段（16种类型）
- schemas.json 6个消费者 last_adapted 更新

## enhance-qa-system Phase 3b 实现完成 (Tasks 7.1-7.5)

### skill.md 完全重写
- 能力边界升级：从"静态数据查询"→"数据分析汇总服务"
- 8种问题类型识别规则（A概览/B数值/C机制/D搭配/E对比/F反查/G公式/H列表）
- 每种类型的调用策略（子命令+参数组合+组合调用示例）
- 16种 response_type 输出格式模板（含 few-shot 示例）
- 完整 CLI 参考文档

## enhance-qa-system Phase 4 实现完成 (Tasks 8.1-8.5)

### 完整性校验与收尾
- `init_knowledge_base.py` 新增 `run_completeness_check()` 函数（Step 7）
  - 分类型实体覆盖率检查：skill summary 96%, passive display_stats 100%
  - 机制库覆盖率+孤立外键检查
  - 辅助匹配完整性+效果分类分布
  - 公式库数量合理性
- 端到端测试 8 种问题类型全部通过
- 清理临时文件：temp_gap_formulas.py, temp_query_test.py, TEMP_FILE_POLICY.md
- version.yaml 更新为 v3.0.0

### 🎉 enhance-qa-system 全部 37 个任务完成
- Phase 1 (1.1-1.4): ✅ 数据层修复
- Phase 2 (2.1-2.6): ✅ StatDescriber 适配层
- Phase 2a (3.1-3.6): ✅ 实体解读层预计算
- Phase 2b (4.1-4.8): ✅ 机制库增强
- Phase 2d (5.1-5.7): ✅ 辅助匹配系统
- Phase 3 (6.1-6.7): ✅ 查询输出层
- Phase 3b (7.1-7.5): ✅ skill.md 重写
- Phase 4 (8.1-8.5): ✅ 完整性校验与收尾

## 代码质量检查与修复

检查 3 个核心文件（init_knowledge_base.py 731行、kb_query.py 1111行、support_matcher.py 953行），发现并修复 4 个问题：

1. 🔴 **运算符优先级Bug** (`init_knowledge_base.py:439`)
   - 原：`if behavior_count * 100 // mech_total < 30 if mech_total else True:`
   - 修：`if (behavior_count * 100 // mech_total < 30) if mech_total else True:`
   - 原因：Python三元表达式 `if C else Y` 绑定到 `30` 而非整个比较

2. 🟡 **版本号不一致** (`init_knowledge_base.py:615,644`)
   - argparse描述和启动banner从 "v2" → "v3"

3. 🟢 **缺失步骤标题** (`init_knowledge_base.py:263`)
   - `compute_support_matching` 添加 "5. 辅助匹配系统" 标题

4. 🟡 **正则内层循环重复编译** (`support_matcher.py:780`)
   - `MECHANIC_SYNERGY_RULES` 的 `support_has_stat` 从原始字符串改为模块级 `re.compile()`
   - `compute_potentials` 中直接使用预编译对象，消除 supports×skills×rules 三重循环中的 re.compile
