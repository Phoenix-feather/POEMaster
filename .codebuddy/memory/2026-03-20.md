# 2026-03-20

## 两项数据质量 Bug 修复

### Bug 1: Flag 型 stat `global_` 前缀不匹配

**根因**：`support_matcher.py` 的 `FLAG_SEMANTIC_MAP` 只有 `cannot_crit` 键，但 entities.db 中 Controlled Destruction 的 stat 是 `global_cannot_crit`，精确匹配失败导致负面效果被静默丢弃。

**修复**：
1. `FLAG_SEMANTIC_MAP` 新增 `global_cannot_crit`、`global_always_hit`、`global_knockback`、`global_maim_on_hit`、`global_poison_on_hit`、`global_bleed_on_hit` 6 个显式条目
2. `_extract_key_stats` 新增 `global_` 前缀 fallback 逻辑：精确匹配失败时自动去掉 `global_` 前缀重试

**影响范围**：6 个唯一 `global_` 前缀 Flag stat，涉及多个辅助宝石

### Bug 2: `_per_` 类型 stat 叠加上下文缺失

**根因**：`_build_formula_impact` 只显示原始系数值（如 `MORE +12%`），不显示叠加条件和上限。Rising Tempest 实际是 `12% per elemental type`（最高 3 层 = 36%），但输出仅显示 12%。

**修复**：
1. `load_data` SQL 新增加载 `display_stats` 字段
2. `_parse_entity_row` JSON 字段列表新增 `display_stats`
3. `_build_formula_impact` 新增 `display_stats` 参数
4. 新增 `_find_per_stat_context()` 方法：检测含 `_per_` 的 stat → 从 `display_stats` 匹配对应描述 → 提取叠加上下文短语（"for each ..."、"per ..."、"up to ..."）
5. 新增 `_extract_stacking_phrase()` 辅助方法：正则提取叠加条件片段

**影响范围**：63 个辅助宝石含 `_per_` 类型 stat，全部自动获得叠加上下文注释

### 验证结果
- Controlled Destruction: `⚠️ 不能暴击` ✅
- Rising Tempest: `MORE +12% (for each skill used recently of a different elemental type)` ✅
- Arakaali's Lust: `MORE +8% (for each poison on the enemy, up to 40)` ✅
- Burgeon I: `MORE +10% (per second spent channelling, up to 40%)` ✅
- Dauntless: `MORE +3% (for each quarter second you have been stationary, up to 45%)` ✅
- supports.db 重建：52,605 兼容对、556 效果、9,988 潜力推荐（数据一致）

### 文件变更
- `support_matcher.py`: FLAG_SEMANTIC_MAP 扩展 + fallback 逻辑 + display_stats 集成 + `_find_per_stat_context` + `_extract_stacking_phrase` 两个新方法
- `supports.db`: 重建
- 清理临时文件：`_run_debug.py`、`_run_rebuild.py`、`_run_arc_query.py`、`_tmp_*.json`

## Explore 模式：DPS 期望倍率设计（`_per_` Cap 语义分析完成）

### 三类 Cap 统一规则

**Type A (伴随 cap stat)**：
- 识别：constant_stats 中存在 `max_XXX` / `maximum_XXX` 伴随 stat
- max = per_value × cap_stat_value
- 例：Arakaali's Lust `8% × 40 stacks = 320%`，Culmination `3% × 10 = 30%`，Cadence `8% × 6 = 48%`

**Type B (display "up to N%")**：
- 识别：display_stats 包含 `"up to N%"`
- max = N%（直接取百分比值）
- 冲突处理：display > stat名中嵌入的数值
- 例：Dauntless `45%`，Burgeon `40%`，Stoicism `20%`（stat名说30%但display说20%，以display为准）

**Type C (无 cap)**：
- 识别：既没有伴随 cap stat，也没有 "up to"
- 使用 PER_STAT_MAX_HEURISTIC 查找表提供合理最大值
- 例：Rising Tempest `12% × 3 types = 36%`，Inhibitor `4% × 5 types = 20%`
- 用户指令："按照最大期望计算即可"

### Stoicism 30% vs 20% 冲突结论
- stat名 `per_second_up_to_30%` 中的 30% 是标识符不可信
- display_stats `"up to 20%"` 权威 → cap = 20%

### Arakaali's "up to 40" 语义结论
- "up to 40" = 40 stacks（无 `%` 符号），不是 40%
- max = 8% × 40 = 320% more

### 5 维度 DPS 期望设计（已确认）
1. 🗡️ Damage: `_final` stats → MORE/LESS 倍率
2. ⏱️ Speed: `_final` speed → MORE/LESS 倍率
3. 🎯 Crit: 仅标注不估值（用户决策方案A）
4. 📡 Coverage: chain `(1+base+added)/(1+base)`, projectile `(base+added)/base`, AoE 仅标注
5. 📊 综合 DPS: 单体 = damage × speed, 总体 = 单体 × coverage

### 混合存储方案（已确认）
- `support_matcher.py` 预计算 → `support_effects.multipliers` JSON 列
- `kb_query.py` 动态计算 coverage（依赖目标技能 base_chains/base_projectiles）

### 实现完成 ✅

**support_matcher.py 变更**：
- 新增 `PER_STAT_MAX_HEURISTIC` 常量表（Type C 启发式最大值）
- 新增 `_resolve_per_cap()` 方法：三类 Cap 解析（A 伴随stat / B display "up to" / C 启发式）
- 新增 `_compute_multipliers()` 方法：5 维 DPS 期望预计算（damage/speed/coverage_add/crit_notes/notes）
- `compute_effects()` 集成 multipliers 计算
- `export_to_db()` 新增 `multipliers TEXT` 列

**kb_query.py 变更**：
- 新增 `_get_skill_base_coverage()` 方法：从 entities.db 提取技能的 base_chains/base_projectiles
- 新增 `_compute_efficiency()` 方法：总效率 = 单体DPS(damage×speed) × 目标数量(chain_ratio×proj_ratio)
- 新增 `_format_efficiency_line()` 方法：格式化效率输出
- DPS 模式和 summary 模式集成 efficiency 字段

**SKILL.md 变更**：
- 卡片模板新增 `📊 期望` 行
- 填槽块新增 `📊 组合效率` 行
- 示例卡片更新

**验证结果（Arc 基准）**：
- Arc base_coverage: `{base_chains: 5, base_projectiles: 1}` ✅
- Controlled Destruction: `x1.25 (伤害 x1.25)` ✅
- Chain I: `x0.82 (伤害 x0.70 | 目标 x1.17 (连锁 6→7))` ✅
- Considered Casting: `x1.15 (伤害 x1.35 | 速度 x0.85)` ✅
- Rising Tempest (Type C): `x1.12~1.36 (伤害 x1.12~1.36)` ✅
- Arakaali's Lust (Type A): `x1.08~4.20 (伤害 x1.08~4.20)` ✅
- Dauntless (Type A): `x1.03~2.35 (伤害 x1.03~2.35)` ✅
- Cadence (Type A speed): `x1.08~1.48 (速度 x1.08~1.48)` ✅

**supports.db 统计**：52,605 兼容对、556 效果、293 可量化、**246 有 multipliers**、9,988 潜力推荐

**schemas.json**: `support_effects` hash 更新为 `v1.1-multipliers`

**清理**：`_tmp_check2.py`、`_tmp_verify.py`、`_tmp_arc_verify.json` 已删除

## 三个 Bug 修复实现完成 ✅

### Bug #1: Dominus' Grasp (Piety's Mercy) chains_more 乘法连锁

**根因**：`chains_hit_X_more_times = 1` 表示 "100% more chain times"（乘法翻倍），但 `_compute_multipliers` 只检查 `number_of_chains`（加法），完全跳过该 stat。`_build_formula_impact` 也错误显示为 "(+1)"。

**修复**：
1. `_compute_multipliers` 新增 `coverage_chains_more` 变量和 `_more_times` / `_more_time` 分支
2. `coverage_add` 输出新增 `chains_more` 字段（1.0 = 100% more = ×2.0）
3. `_build_formula_impact` 新增 `_more_times` 检测，显示为 `"MORE +100% times"`
4. `kb_query._compute_efficiency` 更新连锁计算：`total_chains = (1+base+added) × (1+chains_more)`

**验证**：Arc (5 base chains) + Dominus' Grasp → 连锁 6→12, efficiency x2.00 ✅

### Bug #2 + #3: 通用 stat qualifier 语义有效性系统

**设计**：三维度正交 qualifier 提取 + 四级有效性评分

**support_matcher.py 变更**：
1. 新增三个词典常量：`DAMAGE_TYPE_WORDS`(6)、`ATTACK_MODE_WORDS`(6)、`DAMAGE_SCOPE_WORDS`(7)
2. 新增 `BLOCK_FLAG_MAP`: 10 个 flag stat → 被阻断维度标签集合
3. 新增 `_extract_stat_qualifiers(stat_name)` 静态方法：分词 + 三维度词典匹配
4. 新增 `_extract_restrictions(support)` 方法：扫描 _final stats → requires，扫描 flag stats → blocks
5. `compute_effects()` 集成 restrictions 提取
6. `export_to_db()` 新增 `restrictions TEXT` 列

**kb_query.py 变更**：
1. 新增 `_TAG_TO_DAMAGE_TYPE`、`_TAG_TO_ATTACK_MODE`、`_TAG_TO_DAMAGE_SCOPE` 映射表
2. 新增 `_get_skill_profile(skill_id)` 方法：从 skill_types 标签提取三维度 profile + 推断逻辑（elemental 派生、hit/dot 推断）
3. 新增 `_check_effectiveness(restrictions, skill_profile)` 方法：四级评分（effective/partial/ineffective/fatal）
4. DPS --summary 模式集成：批量读取 restrictions → 检查有效性 → fatal 自动过滤 → ineffective 标注 N/A → 输出 skill_profile

**验证结果（ArcPlayer 基准）**：
- Arc profile: `{damage_types: [elemental, lightning], attack_modes: [projectile, spell], damage_scopes: [hit]}` ✅
- Brutality → Arc: **fatal** (阻断全部伤害类型) ✅
- Controlled Destruction → Arc: **effective** ✅
- Heft → Arc: **ineffective** (physical 增益不适用) ✅
- Chain I → Arc: **effective** ✅
- DPS summary: 136 辅助保留, 113 effective, 2 partial, 18 ineffective (fatal 已过滤) ✅

**supports.db 统计**：52,605 兼容对、556 效果、247 倍率、**124 有语义限制**、9,988 潜力推荐

**schemas.json**: `support_effects` hash 更新为 `v1.2-restrictions`

**清理**：`_tmp_verify_v2.py`、`_tmp_quick_verify.py` 已删除

## Cap 语义修复完成 ✅

### 根因

`_resolve_per_cap` 中 Type A（伴随 cap stat）先于 Type B（display "up to"）执行，且 Type A 使用命名模式区分层数/百分比上限。Arakaali's Lust 的 cap stat 名 `support_debilitate_hit_damage_max_poison_stacks=40` 含 "stacks" 但实际是百分比上限（POB `limitTotal=true`），导致被误解为 40 层 × 8% = 320% → x4.20。

### 修复方案

1. **Type B 优先于 Type A**：display_stats 中的 "up to N%" 是 GGG 官方描述，语义权威性最高
2. **"up to N" 无%号统一为百分比上限**：在 `_per_` 上下文中，"X% more ... up to N" 的 N 是百分比上限（对应 POB `limitTotal=true` 语义）
3. **Type A 简化为纯层数计算**：只在 display 无 "up to" 时生效，cap stat 值一律视为层数（如 Cadence `maximum_stacks=6`）

### 验证结果（全部通过）

| 辅助 | 修复前 | 修复后 | Cap 来源 |
|---|---|---|---|
| Arakaali's Lust | x4.20 (320%) ❌ | **x1.40 (40%)** ✅ | Type B: "up to 40" |
| Dauntless | x2.35 (135%) ❌ | **x1.45 (45%)** ✅ | Type B: "up to 45%" |
| Burgeon I | x1.40 ✅ | x1.40 ✅ | Type B: "up to 40%" |
| Burgeon II | x1.40 ✅ | x1.40 ✅ | Type B: "up to 40%" |
| Stoicism I | x1.20 ✅ | x1.20 ✅ | Type B: "up to 20%" |
| Stoicism II | x1.20 ✅ | x1.20 ✅ | Type B: "up to 20%" |
| Cadence | x1.48 ✅ | x1.48 ✅ | Type A: 6 stacks × 8% |
| Culmination I | x1.30 ✅ | x1.30 ✅ | Type A: 10 stacks × 3% |
| Culmination II | x1.40 ✅ | x1.40 ✅ | Type A: 20 stacks × 2% |
| Cirel's Cultivation | x1.50 ✅ | x1.50 ✅ | Type A: 5 stacks × 10% |
| Encroaching Ground | x2.00 ✅ | x2.00 ✅ | Type B: "up to 100%" |

### 文件变更
- `support_matcher.py`: `_resolve_per_cap` 重排 Type B 优先 + 无%号统一为百分比上限 + Type A 简化
- `supports.db`: 重建（52,605 兼容对、556 效果、247 倍率、124 限制）
- `schemas.json`: `support_effects` hash 更新为 `v1.3-cap-semantic-fix`

## Efficiency 格式修复（方案 A）✅

### 问题
1. 范围格式 `x1.04~1.20` 中 `~` 和数字挤在一起难区分
2. 单维度辅助（230/247=93%）的 efficiency 括号内完全重复括号外数值，如 `x1.04~1.20 (伤害 x1.04~1.20)`

### 修复
**方案 A：最小改动 —— 加空格 + 单维度简化**

`kb_query.py` 变更：
1. `_compute_efficiency` breakdown 构建：范围格式改为 `x{min:.2f} ~ x{max:.2f}`（加空格+x前缀）
2. `_format_efficiency_line` 重写：检测 `|` 判断单/多维度
   - 单维度固定值: `x1.25 (伤害)` — 只保留维度标签
   - 单维度范围: `x1.04 ~ x1.20 (伤害)` — 只保留维度标签
   - 多维度: `x1.15 (伤害 x1.35 | 速度 x0.85)` — 保留完整拆解
   - 无变化: `x1.00 (无倍率变化)`

### 确认：Dauntless 辅助 Arc 非 Bug
POB 源码 `doesTypeExpressionMatch`：无运算符时隐式 OR。Dauntless `requireSkillTypes = [Attack, Damage, CrossbowSkill, CrossbowAmmoSkill]` 中 Arc 有 `Damage` 标签 → 匹配成功。描述 "Supports Skills which can cause Damaging Hits" 也适用于命中法术。

## recommend 组合查询模式 + 临时文件管理规范 ✅

### 背景
之前每次 Type D 辅助推荐需要 3 次独立调用（dps/utility/potential），AI 有时把中间 JSON 存到 `_tmp` 文件避免上下文过大，但用完不清理。

### 改造内容

**方向1 + 方向2: kb_query.py 新增 `--mode recommend`**
- 一次调用返回 dps(summary) + utility + potential 三份数据
- 复用现有查询逻辑（选项A方案：分别调用再合并）
- 精简输出字段：DPS 每条目只保留 id/name/pos/neg/impact/efficiency/note/effectiveness
- utility 只保留 id/name/category/impact
- potential 只保留 id/name/synergy/reason
- 数据量：旧方案 47,687 bytes (3次调用) → 新方案 41,349 bytes (1次调用)

**方向3: 临时文件管理规范**
- SKILL.md 新增「临时文件管理规范」章节
- 固定缓存路径：`.codebuddy/skills/poe-data-miner/cache/`
- 命名规范：`_cache_*` / `_tmp_*`
- 强制清理：每次回答完成后删除 cache/ 下所有临时文件
- 禁止在项目根目录或其他位置创建临时文件

### 文件变更
- `kb_query.py`: argparse 新增 recommend 选项 + `query_supports` 新增 recommend 分支（~80行）
- `SKILL.md`: Type D 调用策略更新（recommend 优先） + 临时文件管理规范章节
- `cache/.gitignore`: 新建，忽略 `_cache_*`、`_tmp_*`、`_test_*`

## 升华效果查询功能实现完成 ✅

### PassiveModParser（Step 1-3，前一 session 完成）
- `passive_mod_parser.py`: 天赋/升华 stat 描述文本解析器
- 从 ModParser.lua 提取 ~40 form patterns + ~200 mod name mappings
- 解析率 99.7%（5,693/5,709），16 条未解析为复杂复合句
- 7 维度分类：damage/speed/crit/defense/resource/utility/qualitative
- entities.db 新增 `parsed_mods` TEXT 列，JSON 格式存储

### kb_query.py 升华查询集成（Step 4）
- `query_ascendancy(name)` 方法：模糊匹配升华名称 → 查询所有节点 → 读取 parsed_mods → 按维度分组 → 返回结构化摘要
- `list_ascendancies()` 方法：返回 20 个升华的节点统计
- CLI `ascendancy` 子命令：`--list` 列表 / `<name>` 查询
- 返回格式：notable_nodes（含 effects 摘要）+ small_nodes + effects_by_dimension（quant/qual 分组）+ summary
- `_format_ascendancy_mod()` 支持全部 mod 类型的人类可读描述生成

### 端到端验证（Step 5）
- 10/10 测试全部通过
- 覆盖：精确/模糊匹配、不存在错误、维度分布、notable 效果格式、量化结构、全 20 升华可查、summary 一致性

### 文件变更
- `passive_mod_parser.py`: 新建（889行）
- `kb_query.py`: 新增 `query_ascendancy` + `list_ascendancies` + `_format_ascendancy_mod` + CLI ascendancy 子命令（~250行）
- `entities.db`: 新增 `parsed_mods` 列（4,313 passive_node 已填充）

## 血法师 Seething 血瓶数学关系分析完成 ✅

### 分析范围
角色最大生命、技能消耗、血瓶回复血量、血瓶充能数、不消耗充能概率之间的关系（仅考虑瞬间回复血瓶）

### POB 数据源
- `flask.lua`: Ultimate Life Flask (life=920, chargesUsed=10, chargesMax=75, quality=20)
- `ModFlask.lua`: Seething 前缀（50% reduced + Instant Recovery）、Ample/Chemist 后缀
- `Item.lua:1638-1672`: 药剂属性计算公式
- `ItemsTab.lua:3255-3310`: 充能展示与uptime计算
- `CalcPerform.lua:1309-1348`: calcFlaskRecovery（FlaskLifeRecovery MORE/INC 应用）
- `ModParser.lua:893-916`: "life recovery from flasks" → FlaskLifeRecovery 映射

### 关键结论
1. Seething + Sanguine Tides 双减益: 920×1.2×0.5×0.5 = **276点瞬回**（仅5000生命的5.5%）
2. 充能获取阈值: 每4%生命=MaxLife×0.04，5000生命时需单次消耗≥200才获1充能
3. Sanguine Tides核心是伤害加成: 40%×C_max的物理伤害%，应选Ample后缀（127充能→50%物伤）
4. Ample vs Chemist同为后缀不可共存
5. 不消耗概率P_nc=20%时约+25%有效使用次数

## Classes/ 目录知识分析完成（Explore 模式）

### 结论：计算公式 + 游戏机制两者都有

**4 个高价值文件（Modules/ 未覆盖的独有知识）**：

| 文件 | 类型 | 行数 | 核心内容 |
|------|------|------|---------|
| `Item.lua` | 计算公式 | 1917 | 物品数值计算：武器DPS(1542-1601)、护甲(1602-1637)、药剂(1638-1672)、calcLocal(1465)、BuildModListForSlotNum(1495) |
| `ModStore.lua` | 核心机制 | 866 | Modifier 聚合引擎：Sum(129)、More(158)、Flag(169)、Override(180)、EvalMod(304) — 整个计算系统的基石 |
| `PassiveSpec.lua` | 游戏机制 | 2366 | 天赋分配逻辑：路径搜索(BFS)、依赖关系、集群珠宝子图、AllocNode(813) |
| `PassiveTree.lua` | 游戏机制 | 806 | 节点 modifier 解析：ProcessStats(400) — 文本→ModList 转换 |

**混合型文件（游戏公式嵌入 UI）**：
- `ItemsTab.lua`(3462行): 药剂回复放大公式(3157-3169)、武器DPS展示计算(2800+)
- `CalcsTab.lua`(686行): CalculateCombinedOffDefStat(655) — 攻防综合评分公式
- `TradeQueryGenerator.lua`(1075行): Modifier 权重评估系统

**纯 UI（~53个文件）**：所有 *Control.lua、大部分 *Tab.lua — 无提取价值

**与 Modules/ 的关系**：Modules/Calc*.lua 处理战斗计算（DPS/防御/触发），Classes/Item.lua 处理物品本地属性计算（是上游），ModStore.lua 是两者共用的基础设施

## Classes/ 白名单集成实现完成 ✅

### 变更摘要

将 4 个含游戏逻辑的 Classes/ 文件纳入现有提取管线（不新建脚本，遵循迭代规则）。

### 文件变更

**pob_paths.py**:
- 新增 `CLASSES_WHITELIST` 常量（Item.lua, ModStore.lua, PassiveSpec.lua, PassiveTree.lua）
- `collect_lua_files()` 新增 Section 5 白名单扫描，总文件数 802→806
- `get_file_scope_summary()` 新增 `classes` 键
- `validate_pob_path()` 新增白名单文件存在性检查

**formula_extractor.py**:
- OOP 冒号语法支持：`ClassName:method()` → formula_id 中冒号替换为下划线
- 3 个新 stat 提取模式：calcLocal(Pattern15)、modList:List(Pattern16)、self.list(Pattern17)
- 验证：Item.lua 22函数、ModStore.lua 20函数、PassiveSpec.lua 38函数、PassiveTree.lua 9函数

**mechanism_extractor.py**:
- 新增 `aggregation` 机制分类
- 5 个 ModStore 聚合机制定义（Sum/More/Flag/Override/EvalMod）
- 6 个机制间关系（stacks_with/overrides/modifies）
- BehaviorExtractor 扩展：Classes/ 目录搜索 + `extract_aggregation_behavior()` 方法

**pob-data-extraction-scope.mdc**: 新增 Classes/ 白名单章节 + 验证清单 4 项

**schemas.json**: mechanisms v2.1、mechanism_sources v2.1、mechanism_relations v1.1、gap_formulas v1.1

### 清理
- `_verify_classes.py` 临时文件已删除

## 知识库完整重建 v3.0.0 ✅

运行 `init_knowledge_base.py`，包含 Classes/ 白名单 + data_scanner P0 修复。

### 数据统计

| 库 | 指标 | 数值 |
|---|---|---|
| **entities.db** | 总量 | **21,686** (前次 16,461) |
| | item_base | 1,171 |
| | mod_affix | 7,795 |
| | stat_mapping | 5,230 |
| | passive_node | 4,313 |
| | skill_definition | 1,248 |
| | gem_definition | 900 |
| | minion_definition | 496 |
| | unique_item | 474 |
| | calculation_module | 59 |
| **formulas.db** | 通用公式 | 40 |
| | stat 映射 | 1,404 |
| | 缺口公式 | 43 |
| | 合计 | **1,487** |
| **mechanisms.db** | 机制 | **76** (前次 44, +32 新机制含 5 个 ModStore 聚合) |
| | 来源 | 75 |
| | 关系 | 25 |
| | category 覆盖 | 100% |
| | behavior 覆盖 | 53% |
| **supports.db** | 兼容对 | 52,605 |
| | 效果 | 556 |
| | 潜力推荐 | 9,988 |

### 覆盖率

| 类型 | summary | display_stats |
|---|---|---|
| mod_affix | 100% | 100% |
| passive_node | 100% | 100% |
| skill_definition | 97% | 72% |
| unique_item | 99% | 99% |

### 完整性校验：✅ 所有检查通过

### 增量变化（对比前次 16,461 实体）
- +5,225 实体（主要来自 data_scanner item_base/mod_affix 修复）
- +32 机制（ModStore 聚合 + stat-based 新增）
- +19 关系（含 6 个 ModStore 聚合引擎内部关系）
- 扫描范围 802→806 文件（+4 Classes/ 白名单文件）

## data_scanner.py P0 提取 Bug 修复 ✅

### 修复内容

**1. `_extract_item_bases()` 重写** — 修复所有子结构提取:

| 子结构 | 修复前 | 修复后 | 受影响数量 |
|--------|--------|--------|-----------|
| `weapon` | 正则 `weaponDamage = {min, max}` (字段名错误) | 提取 `weapon = { PhysicalMin, PhysicalMax, CritChanceBase, AttackRateBase, Range, ReloadTimeBase }` | 331 武器 |
| `armour` | 正则 `armour = (\d+)` (顶层整数，格式错误) | 提取 `armour = { Armour, Evasion, EnergyShield, BlockChance, MovementPenalty }` | 550 防具 |
| `req` | 正则 `reqStr = (\d+)` (字段名错误) | 提取 `req = { level, str, dex, int }` | 839 有需求 |
| `flask` | 完全缺失 | 新增 `flask = { life, mana, duration, chargesUsed, chargesMax }` | 18 药剂 |
| `charm` | 完全缺失 | 新增 `charm = { duration, chargesUsed, chargesMax, buff }` | 13 护符 |
| 新增字段 | 缺失 | `subType`(589), `quality`, `socketLimit`, `implicit`(371), `implicitModTypes`, `charmLimit` | 全部 1186 |

**2. `_extract_mod_affix()` 重写** — 扩展文件兼容性:

| 变更 | 修复前 | 修复后 |
|------|--------|--------|
| type 字段 | 硬性要求 | 可选，默认 `Implicit` |
| ModIncursionLimb | 0 条 | 12 条 ✅ |
| ModRunes | 0 条 | 259 条 ✅ |
| 描述文本 | 空字符串引号偏移 bug | `[^"]*` 修复 |

### 修复后统计
- item_base: 1,186 条目（weapon:331, armour:550, flask:18, charm:13）
- mod_affix: 7,819 条目（Prefix:1324, Suffix:1143, Rune:259, Implicit:4966）

## POB 分享码集成 Spike Step 1 验证通过 ✅

### 目标
验证纯 Python 能否解码 POB 分享码为可读 XML

### 结果
- pobb.in 短链接支持 `/raw` 端点直接获取原始分享码
- 编码格式确认: `URL-safe-Base64(zlib-compress(XML))`，zlib header `78 da` (best compression)
- Python `base64.b64decode()` + `zlib.decompress()` 一步解码成功
- 测试构筑: Level 98 Monk → Invoker (Hybrid Siphon Elements)
- XML 大小: 12,740 字符分享码 → 9,555 字节压缩 → **53,662 字符 XML**
- 根元素: `<PathOfBuilding2>`
- 顶层节点: Build(102 PlayerStats) / Import / Party / Tree(0_4) / Notes / Skills / Calcs(51) / TreeView / Items(21) / Config

### 关键发现
- pobb.in API: `https://pobb.in/{id}/raw` 返回纯文本分享码
- 分享码不能经过 AI 对话传递（12740字符太长，会被截断/损坏）
- zlib 使用标准 header（不是 raw deflate），Python 默认参数即可

### 文件
- `_spike_step1_decode.py`: 解码脚本（从 cache/share_code.txt 读取）
- `cache/share_code.txt`: 原始分享码
- `cache/_spike_build.xml`: 解码后的 XML

### 下一步
Spike Step 2: lupa 加载 POB Data（关键风险点）

## POB 分享码集成 Spike Step 2-6 全部通过 ✅

### Spike 验证结果汇总

| Step | 描述 | 结果 |
|------|------|------|
| 1 | 分享码解码 → XML | ✅ PASS |
| 2 | lupa 加载 POB Lua 运行时 | ✅ PASS (108模块/0错误) |
| 3 | calcs.initEnv (最小 build mock) | ✅ PASS |
| 4 | calcs.perform (计算执行) | ✅ PASS (246个非零输出) |
| 5-6 | 灵敏度分析 (modDB注入→重算) | ✅ PASS |

### Step 3 修复记录

**问题1**: `classStats` nil — `env.classId = env.spec.curClassId`（不是 `classId`）
- **根因**: mock 用 `classId=6` 但 CalcSetup.lua 读 `curClassId`
- **修复**: 改为 `curClassId=7`（Monk 在 tree.lua 是 [8]，PassiveTree.lua 第81-83行转为 0-indexed → [7]）
- **基础属性**: Monk base_str=7, base_dex=11, base_int=11（DexInt混合）

**问题2**: `allocatedMasteryTypes` nil — `copyTable()` 对 nil 调用
- **修复**: 添加 `allocatedNotableCount=0, allocatedMasteryCount=0, allocatedMasteryTypeCount=0, allocatedSmithBodyArmourNodeCount=0, allocatedMasteryTypes={}`

**问题3**: `partyMembers` nil — `env.partyMembers = env.build.partyTab.actor`
- **修复**: `partyTab.actor = { modDB=ModDB, output={}, Aura={}, Curse={}, Warcry={}, Link={} }`

**问题4**: `unpack` nil — Lua 5.4 移除全局 unpack
- **修复**: `if not unpack then unpack = table.unpack end`

### 灵敏度分析验证

```
BASE: Life=1206, Mana=444, DPS=6.06, Evasion=7
+100 flat Life:   1206 → 1306 (delta=+100)    ✅ 精确
+50% inc Evasion: 7    → 11   (delta=+4)      ✅ 正确 (7×1.5≈11)
+20% more Life:   1206 → 1447 (delta=+241)    ✅ 精确 (1206×1.2=1447)
```

三种 modifier 类型（BASE/INC/MORE）均验证通过，灵敏度分析方案可行。

### POB tree.classes 关键架构发现

- tree.lua 原始 classes 是 1-indexed（Lua标准）
- PassiveTree.lua 第81-83行 `for i=0,6 do self.classes[i]=self.classes[i+1] end` 转为 0-indexed
- 所以 Monk tree.lua [8] → 加载后 classes[7]
- XML 中 `classInternalId` 是 integerId（如 Monk=10），通过 `classIntegerIdMap[10]=7` 映射
- `curClassId` 是数组索引（如 7），不是 integerId

### 文件
- `_spike_step3_calc.py`: Step 3-6 完整验证脚本
- 依赖: `cache/share_code.txt`（分享码）、`cache/_spike_build.xml`（XML）

### 下一步
Spike 全部通过。可选方向：
1. 加载完整技能组 + 装备获得真实 DPS 数值（需要实现 gem → gemData 映射和 item 解析）
2. 将 spike 代码整理为正式 skill（`pob-calc-engine`）

## 完整构筑加载验证 — 技能组 + 装备 + 天赋树 + Config ✅

### 新增功能

**`_spike_step3_calc.py` 新增/修改**：

1. **`decode_tree_url(build_info)`** — Python 解码天赋树 URL（ver 6 格式）
2. **`load_tree_into_lua(lua, build_info)`** — 加载 tree.lua（4701 节点）+ 全量 modList 解析
3. **`load_config_into_lua(lua, build_info)`** — Python-Lua 互操作加载 39 个 Config 输入

### 修复的 7 个 Bug

| Bug | 根因 | 修复 |
|-----|------|------|
| gemData 全 nil | XML `/Gem/`(单数) vs data.gems `/Gems/`(复数) | 自动 `/Gem/`→`/Gems/` 修正 |
| mainSkill 选错 | initEnv clamp mainSocketGroup 到 1 | 加载后恢复原始值 |
| classStats nil (tree后) | Monk 在 classes[8]（循环只到6不移动） | `classIntegerIdMap[10]=8` 动态修正 |
| allocMode nil | 节点缺字段 | 初始化 `allocMode=0` |
| sha1 不可调用 | mock 返回 table 非 function | 改为返回函数 |
| TotalEHP = NaN | Config 未加载 | 加载 39 个 Config |
| nameSpec:match 失败 | Lua 5.4 冒号语法 | 改 `string.match()` |

### 验证结果

| 指标 | 结果 |
|------|------|
| 技能组 | 23/23 ✅ | 装备 | 21/21 ✅ | 天赋 | 154/154 ✅ |
| Config | 39/39 ✅ | 非零输出 | 325 个 | TotalEHP | 4541.89 (POB: 9693.78) |
| Life | 1946 (POB: 2180, 89%) | ColdResist | 75=75 (100%) ✅ |
| 灵敏度 | BASE/INC/MORE 三组验证通过 ✅ |

### POE2 classes 0-index 发现

`for i=0,6` 只处理 7 个职业，Monk 在 classes[8] 不被移动。curClassId 通过 classIntegerIdMap 查找。








